# local workshop history (predecessor scripts and machine-specific data) — never published
local-history/

# local tool settings
.claude/

# local-only project memory (Claudio's working notes — never published)
context/
projects/
decisions/

# OS noise
.DS_Store
